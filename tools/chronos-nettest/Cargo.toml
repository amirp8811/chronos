[package]
name = "chronos-nettest"
version = "0.1.0"
edition = "2024"
description = "Local CHRONOS relay experiment harness and smoke scenarios"
publish = false

[dependencies]
chronos-core = { path = "../../crates/chronos-core" }
tokio = { version = "1.37", features = ["macros", "net", "process", "rt-multi-thread", "time"] }
rand = "0.10"
